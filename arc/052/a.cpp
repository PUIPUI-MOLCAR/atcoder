#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<int, int>;
template<class T> inline bool chmax(T& a, T b) { if (a < b) { a = b; return 1; } return 0; }
template<class T> inline bool chmin(T& a, T b) { if (a > b) { a = b; return 1; } return 0; }

const long long INF = 1LL << 60;

string test(int n, string s1, string s2, string s3){

}

int main(){
  int T,N;
  string s1,s2,s3;
  cin >> T;
  for (int i = 0; i < T; ++i){
    cin >> N;
    cin >> s1 >> s2 >> s3;
  }
  
  cout << N << endl;
  return 0;
}
